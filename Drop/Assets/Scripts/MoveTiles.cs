using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveTiles : MonoBehaviour {

    //public float speed;
    //private float startSpeed;
    //public float accelerationStep;

	//// Use this for initialization
	//void Start () {
 //       startSpeed = speed;
	//}
	
	//// Update is called once per frame
	//void FixedUpdate () {

 //       transform.position += Vector3.up * speed * Time.deltaTime;

 //       float playerY = player.transform.position.y;

 //       if (playerY < -90 && playerY > -100)
 //           speed = startSpeed + accelerationStep * 1;
 //       else if (playerY < -190 && playerY > -200)
 //           speed = startSpeed + accelerationStep * 2;
 //       else if (playerY < -290 && playerY > -300)
 //           speed = startSpeed + accelerationStep * 3;
 //       else if (playerY < -390 && playerY > -400)
 //           speed = startSpeed + accelerationStep * 4;
 //       else if (playerY < -490 && playerY > -500)
 //           speed = startSpeed + accelerationStep * 5;
 //   }
}
